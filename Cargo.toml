[package]
name = "ktisis-reader"
version = "0.1.0"
authors = ["chirp (github.com/chrpy)"]
repository = "https://github.com/ktisis-tools/ktisis-reader"
edition = "2021"

[dependencies]
binread = "^2.2.0"
phf = { version = "0.10", features = ["macros"] }